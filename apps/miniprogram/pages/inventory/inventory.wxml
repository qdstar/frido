<view class="list">
  <view class="item" wx:for="{{items}}" wx:key="id">
    <text class="name">{{item.name}}</text>
    <text class="meta">{{item.quantity}}{{item.unit}} · {{item.category}}</text>
  </view>
  <view wx:if="{{!items.length}}" class="empty">暂无库存，去识物添加吧</view>
</view>
